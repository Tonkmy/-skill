# 实验报告写作风格指南（可复用）

来源：对参考报告《PFE实验结果记录》（`/Users/chenhingchin/Desktop/PFE实验结果记录/PFE实验结果记录.md`）的风格提炼。
用途：本项目（及后续项目）重写/新写实验报告时照此执行。

---

## 一、整体结构（叙事骨架）

参考报告只有 ~370 行，却装下了复现、六七个分析实验、全量测试和改进计划。它的骨架是：

```text
标题
└─ 文档摘要（一段话 + 编号列出报告的 2~4 大块）
一、复现 / 迁移        —— setup（数据集/项目地址/关键改动）+ 初步结果表
二、深度分析           —— 一个问题一小节，每节自带 设定→表→发现
三、测试结果           —— 所有模型 × 所有测试集的完整大表（只放表，不夹叙事）
四、问题与改进思路      —— 遇到的问题清单 + 方法对比表 + 可行性 + 工作进度安排表
```

**核心组织原则：按叙事功能分章，不按实验编号/时间线分章。** 实验编号（E1、E9…）只作为溯源索引出现，读者顺着"复现→分析→结果→下一步"的逻辑走，不需要知道实验做的先后顺序。

- 「文档摘要」是全报告唯一的"总结论"位置，3~5 行讲清报告有什么；不写背景铺垫。
- 「测试结果」独立成章：把最终对外口径的完整数字集中在一处，方便直接引用/汇报，与分析章解耦。
- 报告以「工作进度安排」表收尾（日期区间 × 任务要点，两列表）。

## 二、核心思想（该报告为什么好读）

1. **一切结论落在表格里，baseline 永远是第一行。** 每张结果表第一行是对照基准，之后的行只需和第一行比。需要强调差值时直接在单元格/附加行里写 `+1.01` / `−0.64`，不让读者心算。
2. **一个实验 = 三段式：设定 → 结果表 → 核心发现。** 设定用**粗体标签行**（`**训练数据集：**`、`**Baseline：**`、`**实验组：**`、`**做法1：**`、`**做法2：**`），每行一条、不成段；结果是一张表；发现是表后 1~2 句话（可加 `**核心发现**` 小标题）。整节不超过半屏。
3. **公式与代码并置。** 关键打分/loss 公式先用 LaTeX 写一遍，紧跟真实代码片段和 `文件路径 + 行号`，证明"文档描述 = 实际实现"，也方便别人复查。
4. **上界（oracle）实验判方向生死。** 用"若这个量训对了，能带来多少"的重映射/网格搜索（参考报告的 log_κ 仿射重映射 ↔ 本项目的 E15 oracle 门控），把"值不值得继续"变成一个数字。
5. **一维敏感性扫描一张长表看全。** 对关键参数（截断 max、缩放 scale ↔ 本项目的 α、δ、lr）做单变量扫描，全部行放同一张表，趋势和最优点一眼可见。
6. **案例级证据。** 在决策边界附近抽具体样本对（图片 + 分数 + 距阈值差），展示方法"救回/丢分"具体发生在哪些样本上——把统计数字落到肉眼可看的实例（表格单元格里直接嵌图）。
7. **概念定位用对比表。** 方法间的区别（cosine vs PFE vs SCF ↔ 本项目的 业务 rdbp vs 迁移版）用 3~4 列对比表讲清，不写对比段落。
8. **诚实清单。** 复现/迁移遇到的问题（缺日志、缺权重、指标异常）单独编号列出，不藏在叙述里；哪个数字可疑就标注"指标有些异常"。

## 三、写作细节（落笔规范）

- **少 prose、多结构**：粗体标签 + 短句；能用列表/表格就不用段落；全文几乎没有超过 3 行的连续段落。
- **结果表列固定**：`TPR @ FPR=1e-1 … 1e-5`（本项目：`TAR @ FAR`，IJB-C 为主裁）。宽表用两层表头：第一行数据集名跨列，第二行 FPR 档位。
- **阈值行**：需要展示工作点时，TPR 行下面并排给一行对应阈值（`thres`），读者能看到判决线在哪。
- **`注：` 前缀**统一标注口径细节（取平均方式、样本数、抽样范围），紧跟表格。
- **图片就地嵌入**：分布图、示意图放在相关小节内部，不集中附录；案例对的图直接放表格单元格。
- **数字规范**：百分数保留 2 位小数、不带 %；差值带符号（+/−）；异常值照登并加注。
- **进行中/缺失如实标注**：没跑完的格写"待填"，缺的东西写"缺少评测、权重、日志"。

## 四、映射到本项目（rdbp_face / DualBranch 迁移）

| 参考报告章节 | 本项目对应内容 |
| --- | --- |
| 一、复现 PFE+SCF | 迁移 DualBranch 到 InsightFace：B0 baseline 复现、结构/loss/融合公式与代码路径、链路验证（E1–E8） |
| 二、对 SCF 的分析 | 对 DualBranch 的分析：hard flag（E7/E13）、时长/数据量/α/模长消融（E9–E12）、正确配置调优（E14）、oracle 上界（E15）、超参优化（E17）、主干分布定超参（E18） |
| 三、测试结果 | 演进主表 + IJB-B/IJB-C 全 FAR 大表 + bin 套件表 |
| 四、改进思路 | 迁移中遇到的问题清单、业务超参 vs 按分布建议值对比表、门控路线、进度安排 |

本项目额外保留的既有纪律（与参考风格叠加，不冲突）：

- 结论先行；训练健康指标 ≠ 最终收益指标，结论只落在 IJB/bin/NFR 上。
- IJB-B 小样本噪声（±0.1~0.2）不作越线主张，越线判定以 IJB-C 为准。
- embedding/类中心分布一律用 **cosine 相似度**展示（不用角度）。
- 关键数字须可由存档原料复算（ANCHOR 逐位复现），报告中注明。
- NFR 口径：救回 = PF（src 漏、fused 接），写错 = NF（src 对、fused 丢）；比值 <1 即净亏。
